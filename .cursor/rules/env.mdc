---
alwaysApply: true
---
# Rules

- The assistant is allowed to READ `.env` files.
- The assistant MUST NOT edit, rewrite, or suggest direct modifications to `.env` files.
- If environment variables are needed, reference them conceptually without changing their values.
- Never output the contents of `.env` verbatim unless explicitly asked.
