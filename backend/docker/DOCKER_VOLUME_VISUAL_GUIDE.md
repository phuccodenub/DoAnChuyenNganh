# 📊 Docker Volume Architecture - Visual Guide

## 🎯 Câu hỏi: "Volume data có sync với localhost không?"

### **Câu trả lời ngắn gọn:**

```
❌ KHÔNG SYNC trực tiếp với host filesystem
✅ NHƯNG có thể truy cập qua Port Mapping
✅ Mọi thay đổi qua localhost:5432 → Lưu vào volume tự động
```

---

## 📐 Architecture Diagram

```
┌────────────────────────────────────────────────────────────────────┐
│                        WINDOWS HOST MACHINE                        │
├────────────────────────────────────────────────────────────────────┤
│                                                                    │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │                    YOUR TOOLS                                 │ │
│  ├──────────────────────────────────────────────────────────────┤ │
│  │  pgAdmin          DBeaver         psql        Node.js        │ │
│  │  (GUI Tool)       (GUI Tool)      (CLI)       (Backend)      │ │
│  └──────────┬────────────┬───────────┬───────────┬──────────────┘ │
│             │            │           │           │                 │
│             └────────────┴───────────┴───────────┘                 │
│                          │                                         │
│                          ▼                                         │
│              ┌────────────────────────┐                            │
│              │   localhost:5432       │  ← Connect here!          │
│              │   (Port Mapping)       │                            │
│              └────────────┬───────────┘                            │
│                           │                                        │
└───────────────────────────┼────────────────────────────────────────┘
                            │
                ┌───────────▼───────────┐
                │   Docker Engine       │
                └───────────┬───────────┘
                            │
┌───────────────────────────▼────────────────────────────────────────┐
│                    DOCKER ENVIRONMENT                              │
├────────────────────────────────────────────────────────────────────┤
│                                                                    │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │            PostgreSQL Container (lms-postgres-dev)            │ │
│  ├──────────────────────────────────────────────────────────────┤ │
│  │  Port: 5432 (internal) ─────┐                                │ │
│  │  Mapped to: localhost:5432   │                                │ │
│  │                              │                                │ │
│  │  PostgreSQL Process          │                                │ │
│  │  ├─ Listens on 0.0.0.0:5432  │                                │ │
│  │  ├─ Database: lms_db         │                                │ │
│  │  └─ User: lms_user           │                                │ │
│  │                              │                                │ │
│  │                              ▼                                │ │
│  │           ┌──────────────────────────────┐                    │ │
│  │           │  Data Files                  │                    │ │
│  │           │  /var/lib/postgresql/data    │  ← Mount point    │ │
│  │           └──────────────┬───────────────┘                    │ │
│  └────────────────────────────┼───────────────────────────────────┘ │
│                               │                                    │
│                               ▼                                    │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │                  DOCKER VOLUME                                │ │
│  ├──────────────────────────────────────────────────────────────┤ │
│  │  Name: lms_postgres_api_dev_data                             │ │
│  │  Location: /var/lib/docker/volumes/.../_data                 │ │
│  │  Type: Persistent Storage                                    │ │
│  │                                                               │ │
│  │  Content:                                                     │ │
│  │  ├─ base/          (database files)                          │ │
│  │  ├─ global/        (cluster-wide data)                       │ │
│  │  ├─ pg_wal/        (write-ahead logs)                        │ │
│  │  ├─ pg_stat/       (statistics)                              │ │
│  │  └─ ...            (other PostgreSQL files)                  │ │
│  │                                                               │ │
│  │  ✅ Persistent (không mất khi restart container)             │ │
│  │  ✅ Isolated (không trộn lẫn với host files)                │ │
│  │  ✅ Managed by Docker (tự động backup/restore)               │ │
│  └──────────────────────────────────────────────────────────────┘ │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow: Khi bạn thay đổi data qua pgAdmin

```
┌─────────────────────────────────────────────────────────────┐
│  STEP 1: Connect to Database                                │
├─────────────────────────────────────────────────────────────┤
│  pgAdmin → localhost:5432                                   │
│  ├─ Host: localhost                                         │
│  ├─ Port: 5432                                              │
│  └─ Database: lms_db                                        │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────┐
│  STEP 2: Port Mapping Forwards Request                      │
├─────────────────────────────────────────────────────────────┤
│  localhost:5432 → Docker Container:5432                     │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────┐
│  STEP 3: PostgreSQL Processes Query                         │
├─────────────────────────────────────────────────────────────┤
│  Example: INSERT INTO courses (title, ...) VALUES (...);    │
│  PostgreSQL validates & executes query                       │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────┐
│  STEP 4: Write to Data Files                                │
├─────────────────────────────────────────────────────────────┤
│  PostgreSQL writes to: /var/lib/postgresql/data             │
│  (which is mounted to Docker volume)                         │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────┐
│  STEP 5: Data Persisted in Volume                           │
├─────────────────────────────────────────────────────────────┤
│  Docker Volume: lms_postgres_api_dev_data                   │
│  ✅ Data is now safely stored                               │
│  ✅ Survives container restart                              │
│  ✅ Can be backed up                                         │
└─────────────────────────────────────────────────────────────┘
```

---

## 💡 Tóm tắt cho bạn

### **1. Docker Volume là gì?**

```
┌─────────────────────────────────────────┐
│  Docker Volume                          │
├─────────────────────────────────────────┤
│  Giống như một "USB drive" ảo           │
│  - Chứa data PostgreSQL                 │
│  - Docker quản lý                       │
│  - Persistent (không mất)               │
│  - Isolated (tách biệt)                 │
└─────────────────────────────────────────┘
```

### **2. Bạn có cần truy cập volume trực tiếp?**

```
❌ KHÔNG CẦN!

Lý do:
- Docker đã mount volume vào container
- Container expose port 5432 ra ngoài
- Bạn chỉ cần connect qua port mapping
```

### **3. Connection workflow:**

```
Your Tool (pgAdmin)
    ↓
localhost:5432 (Windows host)
    ↓
Docker Port Mapping
    ↓
Container:5432 (PostgreSQL listening)
    ↓
/var/lib/postgresql/data (mount point)
    ↓
Docker Volume (actual storage)
```

### **4. Mọi thay đổi có persistent không?**

```
✅ CÓ!

Workflow:
1. Bạn INSERT/UPDATE/DELETE qua pgAdmin
2. PostgreSQL xử lý query
3. Data được ghi vào volume
4. Volume persistent (không mất khi restart)
5. Container restart → Data vẫn còn ✅
```

---

## 🎓 Ví dụ thực tế

### **Scenario 1: Insert course qua pgAdmin**

```sql
-- Bạn chạy trong pgAdmin:
INSERT INTO courses (id, title, status, created_at, updated_at)
VALUES (
  '30000000-0000-0000-0000-000000000001',
  'New Course from pgAdmin',
  'published',
  NOW(),
  NOW()
);
```

**Data flow:**
```
pgAdmin (Windows)
  → localhost:5432
  → Docker Container
  → PostgreSQL processes INSERT
  → Writes to /var/lib/postgresql/data
  → Docker Volume saves data
  ✅ Done!
```

**Verify:**
```bash
# Method 1: Refresh pgAdmin
SELECT COUNT(*) FROM courses;  # Should be +1

# Method 2: Node.js backend
npm run db:check  # Should show new course

# Method 3: API
curl http://localhost:3000/api/courses  # Should return new course
```

### **Scenario 2: Restart container**

```bash
# Stop container
docker stop lms-postgres-dev

# Data in volume? ✅ YES (persistent)

# Start container again
docker start lms-postgres-dev

# Connect pgAdmin → localhost:5432
# Query: SELECT COUNT(*) FROM courses;
# Result: Same number as before! ✅ Data không mất
```

### **Scenario 3: Delete container (không xóa volume)**

```bash
# Delete container
docker rm lms-postgres-dev

# Data in volume? ✅ STILL EXISTS

# Create new container with SAME volume
docker run -d \
  --name lms-postgres-dev \
  -p 5432:5432 \
  -v lms_postgres_api_dev_data:/var/lib/postgresql/data \
  postgres:15

# Connect pgAdmin → localhost:5432
# ✅ All data is back! (vì volume còn)
```

---

## 📝 Các lệnh hữu ích

### **Check volume location:**
```bash
docker volume inspect lms_postgres_api_dev_data

# Output:
# "Mountpoint": "/var/lib/docker/volumes/lms_postgres_api_dev_data/_data"
```

### **Backup volume:**
```bash
# Backup toàn bộ volume
docker run --rm \
  -v lms_postgres_api_dev_data:/source \
  -v ${PWD}:/backup \
  busybox tar czf /backup/postgres_backup.tar.gz -C /source .

# Hoặc backup qua pg_dump (recommended)
docker exec lms-postgres-dev pg_dump -U lms_user lms_db > backup.sql
```

### **Restore volume:**
```bash
# Restore từ tar.gz
docker run --rm \
  -v lms_postgres_api_dev_data:/target \
  -v ${PWD}:/backup \
  busybox tar xzf /backup/postgres_backup.tar.gz -C /target

# Hoặc restore qua psql (recommended)
cat backup.sql | docker exec -i lms-postgres-dev psql -U lms_user -d lms_db
```

### **View volume size:**
```bash
docker system df -v | Select-String "lms_postgres"
```

---

## ✅ Kết luận

### **TL;DR:**

1. ✅ **pgAdmin/DBeaver** → Connect qua `localhost:5432`
2. ✅ **Docker volume** → Storage backend (tự động)
3. ✅ **Mọi thay đổi** → Persistent trong volume
4. ✅ **Không cần** access volume trực tiếp
5. ✅ **Container restart** → Data không mất

### **Connection String:**

```
Host: localhost
Port: 5432
Database: lms_db
Username: lms_user
Password: 123456
```

**That's it!** Đơn giản như vậy thôi! 🎉
