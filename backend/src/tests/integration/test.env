# Test Environment Variables
NODE_ENV=test
PORT=3001

# Test Database
DATABASE_URL=postgresql://lms_user:123456@localhost:5432/lms_db_test
DB_HOST=localhost
DB_PORT=5432
DB_NAME_TEST=lms_db_test
DB_USER=lms_user
DB_PASSWORD=123456

# Test Redis
REDIS_URL=redis://localhost:6379/1

# JWT (Test)
JWT_SECRET=test-jwt-secret-key
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=24h
JWT_ISSUER=lms-backend-test
JWT_AUDIENCE=lms-frontend-test

# Frontend (Test)
FRONTEND_URL=http://localhost:3001

# Logging (Test)
LOG_LEVEL=error

# CORS (Test)
ALLOWED_ORIGINS=http://localhost:3001,http://localhost:5174
