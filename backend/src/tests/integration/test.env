# Test Environment Variables
NODE_ENV=test
PORT=3001

# Test Database
DATABASE_URL=postgresql://lms_user:123456@127.0.0.1:5432/lms_db_test
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME_TEST=lms_db_test
DB_USER=lms_user
DB_PASSWORD=123456

# Test Redis
REDIS_URL=redis://localhost:6379/1

# JWT (Test)
JWT_SECRET=test-jwt-secret-key
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=24h
JWT_ISSUER=lms-backend-test
JWT_AUDIENCE=lms-frontend-test

# Frontend (Test)
FRONTEND_URL=http://localhost:3001

# Logging (Test)
LOG_LEVEL=error

# Disable cache for integration tests
DISABLE_CACHE=true

# Disable rate limiting and metrics for integration tests
DISABLE_RATE_LIMIT=true
DISABLE_METRICS=true

# CORS (Test)
CORS_ALLOWED_ORIGINS=http://localhost:3001,http://localhost:5174
CORS_ALLOWED_METHODS=GET,POST,PUT,PATCH,DELETE,OPTIONS
CORS_ALLOWED_HEADERS=Content-Type,Authorization,X-Requested-With
CORS_ALLOW_CREDENTIALS=true
